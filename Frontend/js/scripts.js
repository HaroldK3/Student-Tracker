// Example for filling the student table (replace with real API fetch)
document.addEventListener("DOMContentLoaded", function() {
  if(document.getElementById("students-table")){
    // Fetch and render students here
  }
});
